function fruitProcessor(apples, oranges) {
  const a = `${apples} ${oranges}`;
  const juice = `Juice with ${apples} apples and ${oranges} oranges`;
  return juice;
}

//
console.log(fruitProcessor(2, 4));
